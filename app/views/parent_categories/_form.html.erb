<%= form_with(model: parent_category, local: true) do |form| %>
  <% if parent_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(parent_category.errors.count, "error") %> prohibited this parent_category from being saved:</h2>

      <ul>
      <% parent_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :parent_category_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
